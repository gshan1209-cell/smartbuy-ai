# Root dependency entrypoint used by legacy Render service settings.
# Keep backend runtime dependencies available when Render runs:
#   pip install -r requirements.txt
-r backend/requirements.txt

beautifulsoup4>=4.12,<5
playwright>=1.45,<2
pytest>=8,<9
pyarrow>=15.0.0
scikit-learn==1.4.0
